const pippo = 'pippo';
const pluto = 'pluto';
const oldObject = {
  pippo: pippo,
  pluto: pluto
}
const newObject = {
  pippo,
  pluto
};

console.log('Old', oldObject);
console.log('New', newObject);
